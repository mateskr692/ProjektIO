        <!-- Left Nav Section -->
<section class="left">
    <ul class="top-menu border border-dark border-right-0 border-bottom-0 border-top-0 pt-5 list-unstyled">
        <li>
            <a href="@Url.Action("Index", "Home")" class="text-black-50">Home</a>
        </li>
        <li>
            <a href="@Url.Action("About", "Home")" class="text-black-50">About</a>
        </li>
        <li>
            <a href="@Url.Action("Contact", "Home")" class="text-black-50">Contact</a>
        </li>
    </ul>
</section>
<!-- Right Nav Section -->
<section class="right">
    @if (CurrentUser.Identity.IsAuthenticated)
    {
        <a href="@Url.Action("Index","User")" class="user-button button bg-dark radius">@CurrentUser.Name</a>
    }
    else
    {
        <a href="@Url.Action("Login","Account")" class="user-button button bg-dark radius">Log in / Register</a>
    }
</section>
<section class="middle">
    <div class="row logo">
        <img src="../images/icon.png" width="30px" height="30px" />
        <h3>TradeHub</h3>
    </div>
</section>